<h1>Editing post</h1>

<%= form_for @post, :url => board_post_path(@board, @post), :html => { :multipart => true } do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Show-Post', board_post_path(@board, @post) %> |
<%= link_to 'Back-To-Posts', board_posts_path(@board) %>
