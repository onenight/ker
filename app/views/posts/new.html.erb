<div class="page-header">
  <h1>New Post <small> Inspire Everyone </small></h1>
</div>

<div class="row">
  <div class="span8">
<%= form_for @post, :url => board_posts_path(@board), :html => { :multipart => true } do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>
  </div>
</div>

<%= link_to 'Back-To-Posts', board_posts_path(@board), :class => "btn" %>
